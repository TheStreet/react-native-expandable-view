import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        position: 'absolute',
        bottom: 0,
        backgroundColor: '#fff',
        justifyContent: 'center',
    },
    buttonText: {
        position: 'relative',
        bottom: 0,
        padding: 5,
        textAlign: 'center',
    }
});